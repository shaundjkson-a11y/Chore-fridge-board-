name: Install Family Calendar

on:
  push:
    branches: [ main ]
    paths:
      - '.github/workflows/install-family-calendar.yml'
      - 'family-calendar.js'
      - 'family-calendar.css'
      - 'family-calendar-industrial.css'
      - 'family-calendar-industrial-v2.css'
      - 'assets/family-calendar-industrial-bg.svg'
      - 'calendar.html'

permissions:
  contents: write

jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Wire Family Calendar into fridge board
        run: |
          python - <<'PY'
          from pathlib import Path
          import re
          p = Path('index.html')
          s = p.read_text(encoding='utf-8')
          css = '<link rel="stylesheet" href="family-calendar.css?v=20260911-cal2">'
          industrial = '<link rel="stylesheet" href="family-calendar-industrial.css?v=20260911-steam1">'
          industrial2 = '<link rel="stylesheet" href="family-calendar-industrial-v2.css?v=20260911-steam2">'
          js = '<script src="family-calendar.js?v=20260911-cal2"></script>'
          if 'family-calendar.css' not in s:
              s = s.replace('</head>', css + '\n</head>')
          else:
              s = re.sub(r'<link rel="stylesheet" href="family-calendar\.css\?v=[^"]+">', css, s)
          if 'family-calendar-industrial.css' not in s:
              s = s.replace(css, css + '\n' + industrial)
          else:
              s = re.sub(r'<link rel="stylesheet" href="family-calendar-industrial\.css\?v=[^"]+">', industrial, s)
          if 'family-calendar-industrial-v2.css' not in s:
              s = s.replace(industrial, industrial + '\n' + industrial2)
          else:
              s = re.sub(r'<link rel="stylesheet" href="family-calendar-industrial-v2\.css\?v=[^"]+">', industrial2, s)
          if 'family-calendar.js' not in s:
              s = s.replace('</body>', js + '\n</body>')
          else:
              s = re.sub(r'<script src="family-calendar\.js\?v=[^"]+"></script>', js, s)
          p.write_text(s, encoding='utf-8')
          PY
      - name: Commit Family Calendar wiring
        run: |
          if git diff --quiet -- index.html; then
            echo "Family Calendar already wired"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git commit -m "Apply Family Calendar industrial v2"
          git push
